<div class="contrainer">
  <div class="row">
    <div class="col">

    </div>

    <div class="col text-center">
<h2>Zapomniałaś/eś hasła?</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit "Wyślij mi instrukcje odzyskiwania hasła", class: 'btn btn-success'%>
  </div>
<% end %>

<%= render "devise/shared/links" %>
</div>
    <div class="col">
    </div>
  </div>
</contrainer>
<br/>
<br/>